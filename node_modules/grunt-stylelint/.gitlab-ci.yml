stages:
  - test

test-18:
  stage: test
  image:
    name: docker-registry.wikimedia.org/releng/node18-test-browser:latest
    entrypoint: [ "" ]
  script:
    - node --version
    - npm --version
    - npm ci
    - npm test
